#!/bin/bash

case $0 in
    unstable)
        TARGET="unstable"
        ;;
    dev)
        TARGET="dev"
        ;;
    *)
        TARGET="stable"
        ;;
esac

google-chrome-updater --check --target $TARGET &
exec google-chrome-$TARGET "$@"
